package com.hls.sunflower.config;

// Intentionally left empty: rely on MapStruct `@Mapper(componentModel = "spring")` to generate
// a Spring-managed implementation of ProductMapper.
public class ProductMapperConfig {
    // no beans here; MapStruct will produce a @Component implementation
}
